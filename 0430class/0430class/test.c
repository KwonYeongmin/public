#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <Windows.h>

/*
int main() 
{
	
	FILE *apf, *bpf, *cpf;
	char str1[20][30];
	char str2[20][30];
	//파일 오픈
	apf = fopen("..\\data\\a.txt", "r");
	if ((apf == NULL))
	{
		printf("a X");
		return 1;
	}
	bpf = fopen("..\\data\\b.txt", "r");
	if ((bpf == NULL))
	{
		printf("b X");
		return 1;
	}
	cpf = fopen("..\\data\\c.txt", "w");
	if ((cpf == NULL))
	{
		printf("c X");
		return 1;
	}
	//비교
	
}*/
//문제1
//p554

//문제2
//구구단 저장 원하는 만큼 출력

//문제3
/*파일복사 프로그램 만들기
복사할 파일명과 복사 후 생성되는 파일명을 입력 받아서
파일의 내용을 복사하는 프로그램 작성.
단, txt파일뿐만 아니라 이미지 파일 등의 모든 종류의 파일에
다 동일하게 적용할 수 있어야 한다.
복사 진행 과정을 백분율로 화면에 표시해주도록 하고,
한번에 4바이트씩 복사하도록 한다.
(4바이트 이하의 데이터에 대한 처리가 필요)
ex
원본 파일 : source.txt
대상 파일 : dest.txt

		복사중 : 12%
		fseek .. ftell
*/



